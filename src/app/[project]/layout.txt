import { ReactNode } from "react";

interface ProjectLayout {
  children: ReactNode;
}

const layout = ({ children }: ProjectLayout) => {
  return (
    <section>{children}</section>
    // <section className="bg-shaad-400 min-h-screen w-full">{children}</section>
  );
};

export default layout;
